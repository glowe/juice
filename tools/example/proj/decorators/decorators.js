/*
Define your decorators here.

For example:

juice.decorator.define(
    'hello',
    function(that, my, spec) {
        var orig_render = my.render;
        my.render = function() {
            return "Hello " + orig_render();
        };
    });

*/
